/*
 * ÜBUNG 6 – Klasse Konto (Teil 1 von 2)
 * Ziel: eine eigene Klasse mit Attributen und Methoden schreiben.
 *
 * Aufgabe: Vervollständige die Methoden einzahlen, abheben und print.
 * Die Klasse Bank (andere Datei) benutzt diese Klasse.
 */
public class Konto {
    // Attribute
    String inhaber;
    double saldo;

    // TODO: einzahlen(double betrag) -> saldo um betrag erhöhen

    // TODO: abheben(double betrag) -> saldo um betrag verringern,
    //       ABER nur wenn genug Guthaben da ist (sonst Meldung ausgeben)

    // TODO: print() -> "Konto von <inhaber>: <saldo> CHF" ausgeben
}
